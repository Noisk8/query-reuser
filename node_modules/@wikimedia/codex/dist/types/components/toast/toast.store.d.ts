import type { Ref } from 'vue';
/**
 * Global reactive store for toast notifications.
 * Use push/splice as usual. Refs cannot live on the toast object (reactive unwraps them),
 * so declarative slot targets are stored in contentRefsById.
 */
export declare const toastStore: {
    toasts: {
        icon?: string | {
            path: string;
        } | {
            ltr: string | {
                path: string;
            };
            shouldFlip: true;
            shouldFlipExceptions?: string[] | undefined;
        } | {
            rtl: string | {
                path: string;
            };
            ltr: string | {
                path: string;
            };
        } | {
            langCodeMap: Record<string, import("@wikimedia/codex-icons").SimpleIcon | import("@wikimedia/codex-icons").IconFlipForRtl>;
            default: string | {
                path: string;
            } | {
                ltr: string | {
                    path: string;
                };
                shouldFlip: true;
                shouldFlipExceptions?: string[] | undefined;
            };
        } | null | undefined;
        type?: import("../../types").StatusType | undefined;
        id: string;
        autoDismiss?: boolean | number | undefined;
        message?: string | undefined;
        actionButton?: {
            label: string;
            onClick: () => void;
        } | undefined;
        preventUserDismiss?: boolean | undefined;
        onUserDismissed?: (() => void) | undefined;
        onAutoDismissed?: (() => void) | undefined;
    }[];
};
/**
 * Content target refs for declarative toasts, keyed by toast id.
 * The container assigns the placeholder element to the ref so CdxToast can teleport its slot there.
 */
export declare const contentRefsById: Map<string, Ref<HTMLElement | null, HTMLElement | null>>;
/**
 * True while the user is touching/swiping any toast. Used to pause auto-dismiss
 * on other toasts in the stack.
 */
export declare const isUserTouchingToastRef: Ref<boolean, boolean>;
/**
 * True when a ToastContainer has been mounted. Only one is allowed; duplicates
 * are warned and do not render.
 */
export declare const isToastContainerInitialized: Ref<boolean, boolean>;
